% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Class-PopEpidemic.R
\name{newPopEpidemic}
\alias{newPopEpidemic}
\title{Create a new population with epidemic (PopEpidemic)}
\usage{
newPopEpidemic(
  rawPop,
  group_list = NULL,
  indCases = NULL,
  simParam = NULL,
  nThreads = NULL,
  ...
)
}
\arguments{
\item{rawPop}{an object of \code{\link[AlphaSimR:MapPop-class]{MapPop}} or
\code{\link[AlphaSimR:NamedMapPop-class]{NamedMapPop}}}

\item{group_list}{list of groups for the population. Each group is a separate
epidemic. If NULL it is assumed there is only one group}

\item{indCases}{list of 0s and 1s indicating susceptible and first infected
individuals, respectively. If any other number is passed this will cause
an error. If NULL, it is assumed there is one index case per group.}

\item{simParam}{an object of \code{\link{SimParamEpidemic}}}

\item{nThreads}{number of threads to use if OpenMP is available.
If \code{NULL}, the number is obtained from \code{simParam$nThreads}.}

\item{...}{additional arguments used internally (passed to
\code{\link[AlphaSimR:Pop-class]{newPop}})}
}
\value{
Returns an object of \code{\link{PopEpidemic-class}} with a new slot
 `dynamics`. See \code{\link{initDynamics}} for details
}
\description{
Creates an initial \code{\link{PopEpidemic-class}} from an object of
\code{\link[AlphaSimR:MapPop-class]{MapPop}} or
\code{\link[AlphaSimR:NamedMapPop-class]{NamedMapPop}}.
This will use AlphaSimR `newPop` function for
\code{\link[AlphaSimR:Pop-class]{Pop}}. Then, it will also embed relevant
info from SP in it.
}
\examples{
#Create founder haplotypes
founderPop = quickHaplo(nInd=2, nChr=1, segSites=10)

#Set simulation parameters
SP = SimParamEpidemic$new(founderPop, model = "SIR")
means <- rep(0, nchar(SP$model))
vars <- rep(1, nchar(SP$model))
SP$addTraitA(10, mean = means, var = vars, name = c("sus","inf","tol"))

#Create population
pop = newPopEpidemic(founderPop)

}
\seealso{
\code{\link[AlphaSimR:Pop-class]{Pop}},
 \code{\link{SimParamEpidemic}}
}
